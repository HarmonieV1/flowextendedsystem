[build]
  publish = "dist"
  command = "npm install --legacy-peer-deps && npm run build"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[build.environment]
  NODE_VERSION = "20"
